{
  "buildCommand": "cd ubms-frontend && npm install && npm run build",
  "outputDirectory": "ubms-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
